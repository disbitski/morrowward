# Morrowward for Mac

This target is the macOS entry point for Morrowward's shared Apple companion shell. When signed, it uses App Sandbox with outbound network access and user-selected read/write access for explicit backup export and import. It preserves its own private persistent WebKit data store.

See [`../../README.md`](../../README.md) for build and unsigned-local-run instructions.
